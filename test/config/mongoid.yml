test:
  clients:
    default:
      database: fabric_test
      hosts:
        - localhost:27017
      options:
        read:
          mode: :primary
        max_pool_size: 1
  options:
    raise_not_found_error: false
    belongs_to_required_by_default: false
console:
  clients:
    default:
      database: fabric_console
      hosts:
        - localhost:27017
      options:
        read:
          mode: :primary
        max_pool_size: 1
  options:
    raise_not_found_error: false
    belongs_to_required_by_default: false
